# Copy this file to .env.local and fill in the required values.

# NewsAPI key used by /api/headlines
NEWSAPI_API_KEY=your_newsapi_api_key

# Grok API key used for article verification. Falls back to OpenAI credentials
# if this is not set to ease migration.
GROK_API_KEY=your_grok_api_key

# Optional overrides for Grok verification requests. The app defaults to
# grok-4 and a 9000ms timeout if not provided.
GROK_VERIFICATION_MODEL=grok-4
GROK_VERIFICATION_TIMEOUT_MS=9000

# Legacy OpenAI verification variables remain supported as fallbacks.
OPENAI_API_KEY=your_openai_api_key
OPENAI_VERIFICATION_MODEL=gpt-4o-mini
OPENAI_VERIFICATION_TIMEOUT_MS=9000
